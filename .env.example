# =============================================================================
# 견적서 관리 시스템 환경 변수 설정
# =============================================================================

# -----------------------------------------------------------------------------
# Next.js 설정
# -----------------------------------------------------------------------------
# Next.js 애플리케이션의 기본 URL (개발: http://localhost:3000, 배포: 실제 도메인)
NEXTAUTH_URL=http://localhost:3000

# NextAuth.js 보안을 위한 시크릿 키 (랜덤한 32자 이상의 문자열)
# 생성 방법: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-key-here

# -----------------------------------------------------------------------------
# Google OAuth 설정
# -----------------------------------------------------------------------------
# Google Cloud Console에서 OAuth 2.0 클라이언트 ID 생성 후 입력
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# -----------------------------------------------------------------------------
# Supabase 설정
# -----------------------------------------------------------------------------
# Supabase 프로젝트 URL (https://your-project.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon public key (공개 키, 클라이언트에서 사용)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase service role key (서버에서만 사용, 보안 중요)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# -----------------------------------------------------------------------------
# 데이터베이스 설정 (선택사항 - 직접 DB 연결 시)
# -----------------------------------------------------------------------------
# DATABASE_URL=postgresql://username:password@localhost:5432/quote_management

# -----------------------------------------------------------------------------
# 애플리케이션 설정
# -----------------------------------------------------------------------------
# 개발 모드 설정
NODE_ENV=development

# 로그 레벨 (error, warn, info, debug)
LOG_LEVEL=info

# 파일 업로드 최대 크기 (MB)
MAX_FILE_SIZE=10

# -----------------------------------------------------------------------------
# 이메일 설정 (선택사항 - 알림 기능 사용 시)
# -----------------------------------------------------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# -----------------------------------------------------------------------------
# 외부 API 설정 (선택사항)
# -----------------------------------------------------------------------------
# 환율 API 키 (선택사항)
# EXCHANGE_RATE_API_KEY=your-exchange-rate-api-key

# PDF 생성 서비스 설정 (선택사항)
# PDF_SERVICE_URL=http://localhost:3001